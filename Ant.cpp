#include "Ant.h"
#include <string>
#include <vector>

using namespace std;

int solution(int hp)
{
    int answer = 0;

    int admiral = 5;
    int soldier = 3;
    int worker = 1;

    do
    {
        if(hp % admiral != 0)
        {
            
        }
    }while(hp > 0);
    
    
    return answer;
}